//const checkAge = function (age) {
 //   if (age >= 18) {
  //  return Math.abs(age);
 //   } 
//    return age;
//};

//const greeting = funtion (age) {
//if (age >= 18) {
   // console.log("Hello there");
//} else (false) 
 //   console.log("Hey kiddo");
//

//checkAge(18);

const mainPrice = function (amount) { 
    return amount;
};

const priceIncl = function (amount) {
    return amount * 1,21; 
}
return amount;

const totalPrice = function (amount) {
    const makeMainprice = mainPrice(amount);
    const makePriceIncl = priceIncl(amount);
    return priceVAT;
};

console.log(totalPrice(1000));